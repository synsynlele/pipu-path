name: Stage 15 Preview proof formatter

on:
  push:
    branches:
      - agent/stage-15-builder-collaboration-mvp
    paths:
      - ".github/workflows/stage15-preview-format.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/stage-15-builder-collaboration-mvp
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npx prettier --write tests/e2e/stage15-collaboration.spec.ts
      - name: Commit formatting and remove workflow
        run: |
          rm .github/workflows/stage15-preview-format.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore(stage15): format Preview proof"
          git push origin HEAD:agent/stage-15-builder-collaboration-mvp
