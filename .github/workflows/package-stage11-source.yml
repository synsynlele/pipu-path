name: Package Stage 11 source

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap-placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Disabled during the bulk source bootstrap.'
