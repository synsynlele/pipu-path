name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - name: Stage 23 canonical format diagnostic
        run: |
          npx prettier --write src/app/app/page.tsx src/app/discover/page.tsx src/components/pwa/install-prompt.tsx
          echo '===== STAGE23_HOME_BEGIN ====='
          cat src/app/app/page.tsx
          echo '===== STAGE23_HOME_END ====='
          echo '===== STAGE23_DISCOVER_BEGIN ====='
          cat src/app/discover/page.tsx
          echo '===== STAGE23_DISCOVER_END ====='
          echo '===== STAGE23_INSTALL_BEGIN ====='
          cat src/components/pwa/install-prompt.tsx
          echo '===== STAGE23_INSTALL_END ====='
          exit 1

  staging-e2e:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - run: npm run test:e2e
        env:
          E2E_BASE_URL: ${{ vars.E2E_BASE_URL }}
          E2E_STAGE3_EMAIL: ${{ secrets.E2E_STAGE3_EMAIL }}
          E2E_STAGE3_PASSWORD: ${{ secrets.E2E_STAGE3_PASSWORD }}
      - name: Upload browser traces on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-traces
          path: test-results
          if-no-files-found: ignore
