name: Stage 20 Fix Status Schema Once

on:
  push:
    branches:
      - agent/stage-20-opportunity-marketplace

permissions:
  contents: write

jobs:
  fix:
    if: "${{ !startsWith(github.event.head_commit.message, 'fix(stage20): align application status schema name') }}"
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/stage-20-opportunity-marketplace
      - name: Align the DAL with the exported domain schema
        run: sed -i 's/marketplaceApplicationStatusSchema/opportunityApplicationStatusSchema/g' src/modules/opportunities/infrastructure/marketplace-dal.ts
      - name: Remove workflow and commit fix
        run: |
          set -euo pipefail
          rm .github/workflows/stage20-fix-status-schema-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/modules/opportunities/infrastructure/marketplace-dal.ts .github/workflows/stage20-fix-status-schema-once.yml
          git commit -m "fix(stage20): align application status schema name"
          git push origin HEAD:agent/stage-20-opportunity-marketplace
