# Stage 1 has no required external services.
APP_ENV=development
LOG_LEVEL=info

# Introduce service credentials only in the stage that owns the integration.
# Never prefix secrets with NEXT_PUBLIC_.
